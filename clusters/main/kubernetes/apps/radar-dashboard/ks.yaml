apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: radar-dashboard
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/apps/radar-dashboard/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

